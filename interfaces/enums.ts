export enum CheckForBookResult {
  both= "BOTH_EXIST",
  justText= "TEXT_EXISTS",
  neither= "NEITHER",
  error = "ERROR"
}

export enum AudioBookStatus {
  started = "STARTED",
  completed = "COMPLETED",
  error = "ERROR"
}